// Simple feature flags implementation for JavaScript compatibility

export async function isEnabled(orgId, flagName) {
  // Simple implementation - return false for all flags for now
  return false;
}
